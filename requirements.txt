# 고정 버전 = 이 저장소의 테스트가 실제로 통과하는 조합이다.
# (2026-08-27 기준 pytest 379건 통과. 이전에는 고정값이 실제 설치 버전과
#  달라 — 예: numpy 1.26 고정 / 2.2 설치 — fresh clone 이 검증되지 않은
#  환경을 받았다. docs/AUDIT.md D-4 참조)

# 핵심
flask==3.1.3
flask-socketio==5.6.1
flask-cors==6.0.5
python-dotenv==1.2.2
requests==2.34.2
psutil==7.2.2
anthropic==0.116.0
PyYAML==6.0.3
# YARA 내용 기반 악성코드 탐지 (미설치 시 해시 대조만 동작)
yara-python==4.5.4

# ML (Isolation Forest 이상탐지)
scikit-learn==1.7.2
numpy==2.2.6
joblib==1.5.3

# ── 선택 사항 (없어도 데모 fallback 으로 동작) ──
# 패킷 캡처 — 실환경 전용. 미설치 시 packet_analyzer 가 데모 모드로 동작한다.
# pyshark==0.6
# scapy==2.5.0
#
# LSTM Autoencoder (experimental/lstm_autoencoder.py) 사용 시. 용량이 크다.
# tensorflow==2.17.0
# keras==3.5.0
#
# 개발/보안 점검 도구
# pyflakes==3.2.0
# coverage==7.6.1
# bandit==1.7.9
