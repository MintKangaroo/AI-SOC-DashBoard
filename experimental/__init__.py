"""실험적 코드 격리 구역 — 제품 코드가 아니다.

여기 있는 모듈은 다음 중 하나 이상에 해당해서 `modules/` 에서 분리됐다.

- 실데이터로 학습·검증된 적이 없다
- 출력이 어떤 판단 경로에도 연결되어 있지 않다
- 현재 실행 환경에서 아예 동작하지 않는다

**`modules/` 와 `api/` 는 이 패키지를 import 하지 않는다.** 의존 방향은 단방향이며,
`experimental/` 안의 코드가 깨져도 대시보드 동작에 영향이 없어야 한다.

각 모듈의 격리 사유와 복귀 조건은 `experimental/README.md` 를 참조.
"""
